

// First test of lab #1


// math
def calc(a, b of uint, c of int array[2]) of long
	x = a - b * 3 / ( a % (a + 1) + 1 );
	y = "Resilt of math op. is ";
	res = x + y;
end

// if statement (with shift-reduce conflict)
def condition(a of int, b of int) of int
	if a >= b then
		if a > b then
			neg 225;
		else
			225;
	result;
end

// fuction call, slice
def function_call()
	a = 2;
	b = 3;
	myArr;
	myArr[0] = 0;
	myArr[1] = 1;
	myArr[a + b] = 5;
	res = 100 + myArr[a + b] * (a + b);
	arr2 = myArr[2 .. 5];
end

// block
def blockTest()
	a = 2;
	b = 2;
	begin
		if a == b then
			100 + 50;
		neg b + 2; while a < b;
	end

	begin
		counter = 0;
		def inline_func() of string
			while arr[1, 3, 2] < 5
				counter = counter + 1;
				arr[1, 3, 2] = arr[1, 3, 2] + 1;
			end
		end
		inline_func();
	end
end

def callTest()
	tmp[0] = 0;
	tmp[1] = 1;
	x = calc(1, 2, tmp);
end

def loopTest()
	a = 0;
	b = 2;
	while a < b
		a = a + 1;
	end
	a = 0;
	until a >= b
		a = a + 1;
	end
end

def repeatTest()
	a = 0;
	a = a + 1; until a == 1000;
end

def main()
	tmp[0] = 0;
	tmp[1] = 1;
	calc(1, 2, tmp);
	condition(1, 2);
	function_call();
	blockTest();
	callTest();
	loopTest();
	repeatTest();
end
